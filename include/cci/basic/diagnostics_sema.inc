DIAG(err_integer_literal_too_large, ::cci::Severity::Error, "integer literal is too large")
DIAG(ext_no_type_for_integer_literal, ::cci::Severity::Extension, "integer literal is too large to fit in any standard type; leaving it as unsigned long long")
